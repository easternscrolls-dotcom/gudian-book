# 爬虫静态快照：按方案架构由 R2 + Cloudflare Worker 分流提供，不部署到 Pages
snapshot/

# wrangler 本地状态（部署缓存 / 本地 R2 存储），不进仓库
.wrangler/
worker/.wrangler/

# 上传脚本生成的进度 / 清单文件（运行时产物，非源码）
snapshot-upload-progress*.txt
snapshot-uploaded*.txt
snapshot-upload-failures.txt
upload*.log
probe.txt


